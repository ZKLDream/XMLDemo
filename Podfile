# Uncomment this line to define a global platform for your project
platform :ios, '7.0'

pod 'Ono' #GDataXML——MRC——Google
pod 'hpple' #既可以解析XML也可以解析HTML
pod 'GDataXML-HTML' #对GDataXML重写了的，可以对XML/HTML都可以解析

